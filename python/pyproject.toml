[project]
name = "adeu"
version = "2.1.0"
description = "Automated DOCX Redlining Engine"
authors = [
    { name = "Mikko Korpela" },
    { name = "Uzair Ahmed" }
]
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
keywords = ["docx", "redlining", "llm", "mcp", "agent", "virtual-dom"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
]
dependencies = [
    "python-docx>=1.1.0",
    "structlog>=24.0.0",
    "pydantic>=2.12",
    "lxml>=5.0.0",
    "diff-match-patch>=20230430",
    "jinja2>=3.1.6",
    "pywin32; sys_platform == 'win32'",
    "rapidfuzz>=3.14.5",
    "regex>=2024.11.6",
]

[project.urls]
Homepage = "https://adeu.ai"
Repository = "https://github.com/dealfluence/adeu"
"Bug Tracker" = "https://github.com/dealfluence/adeu/issues"

[project.scripts]
adeu = "adeu.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "mypy",
    "hypothesis",
    "pytest-cov>=7.1.0",
    "pytest-xdist>=3.8.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/adeu"]
exclude = [
    "src/adeu/server.py",
    "src/adeu/mcp_components/doc_cache.py",
    "src/adeu/mcp_components/resources/**",
    "src/adeu/mcp_components/tools/**",
]
artifacts = [
    "src/adeu/assets/*.js",
    "src/adeu/assets/*.png",
    "src/adeu/templates/*.html",
    "src/adeu/assets/*.svg",
    "src/adeu/mcp_components/build_info.json",
]
[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "W"]
ignore = []

[tool.mypy]
python_version = "3.12"
strict = false
ignore_missing_imports = true
check_untyped_defs = true

[[tool.mypy.overrides]]
module = "diff_match_patch.*"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
# Parallel by default. --dist loadgroup pins the xdist_group markers from
# conftest.py (live-Word COM tests) onto ONE worker so they run sequentially
# while everything else distributes; two workers driving the live Word
# instance concurrently crash each other's COM sessions (0x80010108).
# Debugging opt-out: `uv run pytest -n 0 ...` restores serial in-process runs.
addopts = "-n auto --dist loadgroup"
